# TODO - Explore Deals For You Responsive

- [ ] Update `_buildSectionTitle()` in `lib/main.dart` so font size follows screen width (responsive) using `MediaQuery` + `clamp`.
- [ ] Run `flutter test` (optional) and ensure no layout regressions.

